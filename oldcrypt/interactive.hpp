#include <iostream>
#include <string>

#ifndef __interactive_shell
#define __interactive_shell

class my_shell{
  private:
    hoge
  public:
};

#endif
//menu exec_proc socket crypt disas
/*
 * interactive shell
 * menu
 * > "num or string"
 *
 * #socket
 * host >
 * port >
 * message >p64()+p32()+string
 * recv >
 * message >
 * recv >
 * 
 * #crypt(rot13,viju2d,..)
 * crypt(??) > encode(plain)
 * crypt(??) > decode(enc,key)
 * crypt(??) > try_decode(enc,key,if_in_word) key can be empty
 *
 * #exec_proc
 * cmd(raw) > ls -al > filename
 * cmd(raw) > ls -al
 * cmd(result) > string..
 *
 * #disas
 *
 */

